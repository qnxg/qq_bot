[package]
name = "deploy"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
kovi.workspace = true
once_cell = "1.21.3"
serde = { version = "1.0.228", features = ["derive"] }
toml = "0.9.8"
tokio = { version = "1", features = ["process", "sync", "time", "fs", "io-util"] }
tracing = "0.1.41"
